import {ChevronLeft,ChevronRight,Star} from 'lucide-react';import {useEffect,useState} from 'react';import {reviews} from '../data/menu';export default function ReviewCarousel(){const [i,setI]=useState(0);useEffect(()=>{const t=setInterval(()=>setI(x=>(x+1)%reviews.length),5000);return()=>clearInterval(t)},[]);return <div className="review-wrap"><div className="stars">{[1,2,3,4,5].map(x=><Star key={x} fill="currentColor" size={18}/>)}</div><blockquote>{reviews[i][0]}</blockquote><strong>— {reviews[i][1]}</strong><div className="review-nav"><button onClick={()=>setI((i-1+reviews.length)%reviews.length)}><ChevronLeft/></button><div>{reviews.map((_,x)=><i key={x} className={x===i?'active':''}/>)}</div><button onClick={()=>setI((i+1)%reviews.length)}><ChevronRight/></button></div></div>}