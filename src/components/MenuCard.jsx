import {Plus} from 'lucide-react';import {motion} from 'framer-motion';import {useCart} from '../context/CartContext';export default function MenuCard({item}){const {add}=useCart();return <motion.article className="menu-card" whileHover={{y:-6}}><div className="menu-img"><img src={item.img} alt={item.name}/><span className={item.veg?'veg':'nonveg'}>{item.veg?'VEG':'NON-VEG'}</span></div><div className="menu-info"><div><h3>{item.name}</h3><p>{item.desc}</p></div><strong>₹{item.price}</strong></div><button className="add-btn" onClick={()=>add(item)}><Plus size={17}/> Add</button></motion.article>}