import {Link,NavLink} from 'react-router-dom';import {Menu,X,ShoppingBag} from 'lucide-react';import {useState} from 'react';import {useCart} from '../context/CartContext';
export default function Navbar(){const [open,setOpen]=useState(false);const {count}=useCart();const links=[['Home','/'],['Menu','/menu'],['About','/about'],['Gallery','/gallery'],['Contact','/contact']];return <header className="navbar"><Link className="brand" to="/" onClick={()=>setOpen(false)}><span className="brand-mark">🦋</span><span>Butterfly <small>CAFE</small></span></Link><nav className={open?'nav-links open':'nav-links'}>{links.map(([n,p])=><NavLink key={p} to={p} onClick={()=>setOpen(false)}>{n}</NavLink>)}<Link className="nav-order" to="/order" onClick={()=>setOpen(false)}><ShoppingBag size={17}/> Order {count>0&&<b>{count}</b>}</Link></nav><button className="menu-toggle" onClick={()=>setOpen(!open)} aria-label="menu">{open?<X/>:<Menu/>}</button></header>}